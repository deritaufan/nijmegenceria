<li class="collection-item avatar">
  <%= image_tag(player.avatar? ? player.avatar.url : "default_profile.png", class: "circle") %>
  <span class="title"><%= player.name %></span>
  <p><%= pluralize(0, 'game') %> played.
  <div class="fixed-action-btn horizontal click-to-toggle secondary-content" style="bottom: 45px; right: 24px;">
    <a class="btn-floating btn-large teal" id="menu_edit">
      <i class="material-icons">menu</i>
    </a>
    <ul>
      <li><a class="btn-floating red"><i class="material-icons">delete_sweep</i></a></li>
      <li>
        <%= link_to(edit_player_path(player), class: "btn-floating blue") do %>
          <i class="material-icons">mode_edit</i>
        <% end %>
      </li>
    </ul>
	</div>
</li>
			    